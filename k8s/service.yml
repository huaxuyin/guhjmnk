apiVersion: v1
kind: Service
metadata:
  name: guhjmnk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "guhjmnk"
    port: 8080
  selector:
    app: guhjmnk
